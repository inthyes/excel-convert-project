import React from "react"
import { Link } from "gatsby"

export default function NotFoundPage() {
  return (
    <div>
      <h1>잘못된 접근입니다.</h1>
      {/* <Link to={`/`}></Link> */}
    </div>
  )
}
